#include "main.h"

/**
 * _abs- calculate theabsolute value of an integer
 * @i: input integer
 * Return:  absolute value of an integer
 */
int _abs(int i)
{
	if (i < 0)
		return (-i);
	return (i);
}
