#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * _strcmp -  compares two strings.
 * @s1: first string
 * @s2: second string
 * Return: an integer
 */
int _strcmp(char *s1, char *s2)
{
	char r = strcmp(s1, s2);

	return (r);
}
