#include "main.h"
#include <string.h>
/**
 * _strcpy - copy string
 * @dest:parameter
 * @src:parameter
 * Return: results
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
