#include "main.h"
/**
 * _abs - uinction to convert to an absolute number
 * @n: number to convert
 * Return: 1 on success or 0
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
